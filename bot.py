The complete updated bot.py containing all previous features plus the new free URL Shortener (DB, functions, /short command, Mini App support, web server redirect for /s/ codes, menu button, etc). The code is the one from the local workspace after all edits in this session.